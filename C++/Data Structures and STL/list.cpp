#include <list>
#include <iostream>

int main(){
/*
std::list<DataType> myList; // Create an empty list of type T
std::list<DataType> myList(n); // Create a list with n default-initialized elements
std::list<DataType> myList(n, value); // Create a list with n elements initialized to value
std::list<DataType> myList{value1, value2, ...}; // Create a list with a specific initializer list
std::list<DataType> myList(otherList); // Copy constructor
std::list<DataType> myList(std::move(otherList)); // Move constructor
*/

std::list<int> MyList(10);

return 0;
}