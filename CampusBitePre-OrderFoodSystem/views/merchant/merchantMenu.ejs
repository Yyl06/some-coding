<!DOCTYPE html>
<html>
<head>
  <title>Your Shop Menu</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <header class="cb-topbar">
    <div class="cb-topbar-inner">
      <a class="cb-brand" href="/dashboard">CampusBite</a>
      <nav class="cb-nav">
        <a class="cb-nav-link" href="/dashboard">Dashboard</a>
        <a class="cb-nav-link" href="/foods">Manage Foods</a>
        <a class="cb-nav-link" href="/auth/profile">Profile</a>
        <a class="cb-nav-link" href="/auth/logout">Logout</a>
      </nav>
    </div>
  </header>

  <main class="page">
    <div class="cb-hero">
      <div>
        <h1>Your Shop Menu</h1>
        <p class="cb-subtitle">Preview your menu like customers see it.</p>
      </div>
    </div>

    <div class="cb-layout">
      <aside class="cb-sidebar">
        <form method="GET" action="/menu/<%= merchantId %>">
          <label>Search</label><br>
          <input type="text" name="search" placeholder="Search food..." value="<%= search %>">

          <br><br>

          <label>Category</label><br>
          <select name="category">
            <option value="">All Categories</option>

            <option value="Rice"
              <%= category === "Rice" ? "selected" : "" %>>
              Rice
            </option>

            <option value="Drink"
              <%= category === "Drink" ? "selected" : "" %>>
              Drink
            </option>

            <option value="Snack"
              <%= category === "Snack" ? "selected" : "" %>>
              Snack
            </option>
          </select>

          <br><br>
          <button type="submit">Apply filters</button>
          <a class="ml-2" href="/dashboard">Back</a>
        </form>
      </aside>

      <section class="cb-main">
        <% if (!foods || foods.length === 0) { %>
          <div class="card cb-empty">
            <h3>No foods found.</h3>
            <p class="cb-muted">Try changing your search or category.</p>
          </div>
        <% } else { %>
          <div class="cb-grid">
            <% foods.forEach(food => { %>
              <div class="cb-card">
                <% if (food.image) { %>
                  <img class="cb-card-media" src="<%= food.image %>" alt="<%= food.name %>">
                <% } else { %>
                  <div class="cb-card-media"></div>
                <% } %>
                <div class="cb-card-body">
                  <p class="cb-card-title"><%= food.name %></p>
                  <p class="cb-card-meta">
                    <span class="cb-badge cb-badge--primary">RM <%= food.price %></span>
                    <span class="cb-badge cb-badge--muted"><%= food.category %></span>
                  </p>
                  <p class="cb-muted"><%= food.description %></p>
                  <p class="cb-muted">
                    <strong>Status:</strong>
                    <% if (food.availability) { %>
                      <span class="cb-badge cb-badge--primary">Available</span>
                    <% } else { %>
                      <span class="cb-badge">Unavailable</span>
                    <% } %>
                  </p>
                </div>
              </div>
            <% }) %>
          </div>
        <% } %>
      </section>
    </div>
  </main>

  <script src="/js/cb-ui.js" defer></script>

</body>
</html>
